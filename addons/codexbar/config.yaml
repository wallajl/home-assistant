name: "CodexBar"
version: "0.5.3"
slug: "codexbar"
description: "OpenAI Codex and Claude subscription usage in one Home Assistant panel"
url: "https://github.com/wallajl/home-assistant"
arch:
  - amd64
  - aarch64
init: false
stage: experimental
ingress: true
ingress_entry: /
ingress_stream: true
panel_icon: mdi:chart-line
panel_title: CodexBar
map:
  - type: addon_config
    read_only: false
options:
  codexbar_version: "0.41.0"
  refresh_interval: 60
  history_interval: 300
  request_timeout: 90
  log_level: "info"
schema:
  codexbar_version: str
  refresh_interval: int(0,3600)
  history_interval: int(60,3600)
  request_timeout: int(5,120)
  log_level: "list(trace|verbose|debug|info|warning|error|critical)"
